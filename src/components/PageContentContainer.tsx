import React, { FunctionComponent } from "react"

const PageContentContainer: FunctionComponent = ({ children }) => (
    <div className="w-full px-4 pt-48 lg:px-0">{children}</div>
)

export default PageContentContainer
